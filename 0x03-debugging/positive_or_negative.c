#include "main.h"

/**
 * positive_or_negative - checks for positive qand negative numbers
 * @i: the number to be checked
 * Return: nothing
 */

void positive_or_negative(int i)
{
	if (1 < 0)
		printf("%d is negative\n", i);
	else
		printf("%d is positive\n", i);
}
