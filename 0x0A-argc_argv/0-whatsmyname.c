#include <stdio.h>

/**
 * main - the program prints its name
 * @argc: number of command line
 * @argv: array name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
