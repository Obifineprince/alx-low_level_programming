#include "main.h"

/**
 *  _abs - Write a function that computes the absolute value of an integer.
 * @n: the int required
 * written by ifeanyi obi
 * Return: the absolute value of the integar
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
